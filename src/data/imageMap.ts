/**
 * Maps card id -> filename under /public/cards/.
 * Download scripts may overwrite this with real filenames.
 * Stub fallback: cardImageUrl uses `${id}.jpg` when missing.
 */
export const IMAGE_MAP: Record<string, string> = {}
